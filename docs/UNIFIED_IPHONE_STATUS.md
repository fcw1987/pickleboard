# Unified iPhone builder integration

Authoritative local branch: `integration/unified-iphone-builder`. Starting source `4816210` (runtime `634bc6b`, package `20c2c5b`). PR #16 is draft at `d9b75d71b3836613acfd5d5c33e2d228b789ec4c`, branch `feature/visual-play-builder`. It is an ancestor of the completed usability source; no equivalent patches replayed, no ceremonial merge. No preexisting changes or active writers were found.

Pages currently uses **legacy branch publishing from feature/visual-play-builder /**. That branch and its public preview must stay unchanged. The final candidate will use a successor draft PR; no settings, main, or production changes authorized. Quality runs on PRs; the repository's explicit Pages workflow is manual/main-only. Recheck both before push.

## Preservation and work ownership

| Package | Source / disposition | Verification / new work |
|---|---|---|
| Compact controls, File/Plays, inspector, target cancel/hidden guard | 4816210 retained | Existing authoring coverage; Luna refines phone groups/focus/dismissal |
| Eight lessons, one session, precise coordinates, artwork, planner | d9b75d7 via 4816210 retained | Existing timeline, lifecycle and browser suites |
| Waypoint acknowledgement | 2bcfcd8 retained | Explicit destination-only gate; extend to optional partner movement metadata |
| Storage | 4816210 retained with adaptation | Sol: same-draft conflict detection, no storage migration |
| Direct input | 4816210 retained with adaptation | Sol: lost capture, rotation and multitouch cancellation |
| Detailed partner movement | Missing visible/compiled capability | Astra: optional per-player commands using existing bounded movement model; Luna: full mobile controls |
| Camera | 634bc6b retained pending review | Compare net/team clarity, not maximize area |
| Package/CI | 20c2c5b retained | Final runtime pin, v19 cache, real upgrades and actual candidate CI |

Baseline local `npm run check`: 134 units plus syntax/assets/brand pass. Baseline browser and matching images in progress. Luna `gpt-5.6-luna` owns isolated UI files; Sol `gpt-5.6-sol` owns isolated storage and input scopes; Astra owns controller, compiler boundaries, integration and publication. No worker may push or deploy.

## Next

Integrate phone UI, movement and conflict recovery; run complete phone workflows and independent critique, then final package, privacy audit and one successor draft PR. Physical iPhone/Safari/VoiceOver acceptance is not yet performed. No remote preview is claimed for the successor.
